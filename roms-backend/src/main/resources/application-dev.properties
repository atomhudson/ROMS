# ═══════════════════════════════════════
# Dev Profile — Local Development
# ═══════════════════════════════════════

# DevTokenController generates RS256 tokens with its own key pair.
# JwtIdentityProvider auto-registers the dev public key at startup.

# JWT Config (matches DevTokenController defaults)
oms.security.jwt.issuer=oms-dev
oms.security.jwt.audience=oms-api
